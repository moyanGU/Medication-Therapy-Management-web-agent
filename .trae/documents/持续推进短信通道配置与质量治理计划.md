## 目标
- 使短信通道（Spug）在生产环境稳定可用，诊断可见、异常可告警。
- 完成前后端质量治理的落地：增量 lint、构建守护、异常阈值提示。
- 输出架构与测试交付物（在你确认后开始生成文档，不会现在创建文件）。

## 项目现状速览
- 诊断端点已扩展，前端设置页可查看 Web Push 与 Spug 配置状态与问题列表（backend/apps/core/views.py:138-185；src/pages/SettingsPage.vue:415-424, 491-504, 539-564）。
- 提醒页面已支持指标阈值告警与 CSV 导出（src/pages/RemindersPage.vue:257-366）。
- 通道升级与失败兜底逻辑已覆盖：scheduler 与 notifications（backend/apps/reminders/scheduler.py:65-138；notifications.py:62-87, 140-252）。
- 单元测试覆盖 Spug 成功/模板缺失/异常/超时（backend/apps/reminders/tests/test_sms_spug_integration.py）。

## 范围与交付
- 生产化配置方案与校验流程（Spug 模板/令牌/超时）。
- 前端与后端质量治理：增量 lint、构建守护、异常提示统一。
- 文档交付（在你确认后生成）：架构图、接口契约、测试报告与性能指标说明。

## 实施步骤
### Phase 1: 短信通道生产配置
- 设置 .env 变量：`SPUG_TEMPLATE_ID`、`SPUG_PUSH_TOKEN`、`SPUG_PUSH_TIMEOUT_SECONDS`（backend/.env.example:71-76 已对齐键名）。
- 校验路径：前端设置页点击“刷新诊断”，或直接 `GET /api/diagnostics/`；确认 `sms_spug.enabled=true`、`template_configured=true`、`token_configured=true`。
- 验收：提醒调度失败时自动升级到短信；测试用例保持通过；诊断端点返回无关键问题。

### Phase 2: 质量治理（增量）
- Lint 治理策略：按模块分批 `eslint --fix`（先 pages/services/stores），修复后执行 `npm run build` 验证，避免一次性全仓库大改。
- 构建守护：保留现有脚本 `npm run build` 与 postbuild 资源拷贝；在 CI 中串联类型检查 + 构建，失败即阻断。
- 行尾策略（可选）：如后续需要缓和 Windows CRLF 与 Prettier 冲突，再评估 `.prettierrc` endOfLine 或 `.gitattributes`（需你确认后再改）。

### Phase 3: 指标与监控
- 后端在 `/reminder-history/metrics/` 增加可选 `warnings: string[]` 字段，当响应率<60%或失败率>20%时给出原因与建议；前端提醒页面读取并展示（保持已实现的红色告警横幅一致）。
- 导出报表：保留 CSV 导出；补充按周/月分段导出（可选）。
- 验收：接口与页面一致显示阈值告警；日志可追踪。

### Phase 4: 文档与测试交付
- 架构与接口文档：模块关系图（SW→respond_by_subscription→历史→调度器→升级）、数据流与异常策略、接口契约四要素整理。
- 测试报告：包含 Spug 成功/失败/超时、调度 `pending` 消费、升级成功率等；注明 3 次失败停止原则的执行情况。
- 交付前不创建文档文件；你确认后再生成到 `docs/任务名/` 路径。

## 验收标准
- 诊断端点显示 Spug 配置完整且无关键问题；提醒失败场景能升级到短信；页面告警与报表可用。
- 构建与类型检查稳定通过；增量 lint 不引入大规模无关变更。
- 测试全部通过；日志关键路径可定位异常。

## 风险与应对
- 外部 Spug 服务不可用：保留邮箱/Push 备用；诊断端点给出问题条目；页面提示异常。
- Windows CRLF 导致格式化大量变更：采用增量治理，避免一次性全仓库 `--fix`。

## 下一步
- 请确认继续按以上计划推进；确认后我将依序执行 Phase 1→3，并在关键节点输出结果与（你同意后）生成文档交付。
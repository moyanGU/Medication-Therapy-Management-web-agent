# 待办事项：AI 助手完善与集成

## 1. 后端对接
- [ ] **启动后端服务**: 确保后端运行在 `http://127.0.0.1:8000`。
- [ ] **验证接口**: 确认 POST `/ai/medication-guidance/` 可用且返回格式正确。

## 2. 功能增强 (Nice to have)
- [ ] **流式响应**: 目前代码等待完整响应，未来可升级为 Stream 模式以提升体验。
- [ ] **上下文记忆**: 目前仅发送单条 `question`，未来可发送 `messages` 数组以支持多轮对话。
- [ ] **移动端适配**: 进一步测试在不同尺寸手机上的拖拽体验（目前已添加 `touch` 事件支持）。

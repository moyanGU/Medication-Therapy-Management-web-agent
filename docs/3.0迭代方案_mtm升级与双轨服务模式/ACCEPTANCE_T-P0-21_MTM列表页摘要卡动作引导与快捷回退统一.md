# ACCEPTANCE_T-P0-21_MTM列表页摘要卡动作引导与快捷回退统一

## 一、任务结论

`T-P0-21 MTM 列表页摘要卡动作引导与快捷回退统一` 已完成本轮最小可交付目标：

- 已在 MTM 列表页摘要卡中增加统一动作引导区
- 已让摘要卡动作随预设来源和筛选场景自动切换
- 已让空态按钮复用同一套动作定义
- 已完成前端构建验证

---

## 二、本轮完成项

### 2.1 文档对齐

已新增：

- `ALIGNMENT_T-P0-21_MTM列表页摘要卡动作引导与快捷回退统一.md`
- `CONSENSUS_T-P0-21_MTM列表页摘要卡动作引导与快捷回退统一.md`

当前已明确：

- 本轮只做列表页顶层动作统一
- 不新增后端接口
- 不新增筛选和列表项操作能力

### 2.2 共享动作能力

已修改：

- `src/types/mtm.ts`
- `src/utils/mtm.ts`

当前已补齐：

1. MTM 列表页快捷动作类型
2. 列表页统一动作生成函数

当前统一动作集合包括：

1. 刷新列表
2. 清空筛选
3. 看全部服务
4. 回首页查看

### 2.3 摘要卡动作区

已修改：

- `src/pages/MtmServiceCasesPage.vue`

当前摘要卡已支持：

1. 根据预设来源显示动作
2. 根据手动筛选场景显示动作
3. 根据默认场景显示动作
4. 最多展示 2 个主动作

当前动作优先级已实现：

1. 预设来源场景：
   - 看全部服务
   - 回首页查看
2. 手动筛选场景：
   - 清空筛选
   - 刷新列表
3. 默认场景：
   - 刷新列表
   - 回首页查看

### 2.4 空态动作统一

当前空态区域已完成：

1. 不再单独定义独立按钮逻辑
2. 直接复用摘要卡同一套动作定义
3. 与当前页面上下文保持一致

说明：

- 页面不同区域的“下一步动作”现在已统一口径
- 不会再出现摘要卡一套、空态一套的情况

---

## 三、验证结果

### 3.1 编辑文件诊断

已检查：

- `src/pages/MtmServiceCasesPage.vue`
- `src/utils/mtm.ts`
- `src/types/mtm.ts`

结果：

- 无新增诊断错误

### 3.2 前端生产构建

已执行：

```bash
npm run build
```

结果：

- 构建成功
- `vue-tsc` 通过
- `vite build` 通过
- `MtmServiceCasesPage` 与 `MtmServiceCaseDetailPage` 构建产物正常生成

说明：

- 当前动作统一逻辑已通过类型检查和生产构建

---

## 四、已知限制

本轮明确未做：

- 列表项卡片内部动作统一
- 页面头部按钮完全收口到摘要卡
- 摘要卡动作增加更多场景分支
- 更复杂的动作推荐策略

原因：

- 本任务只负责列表页顶层动作统一
- 这些内容属于后续体验增强

---

## 五、验收判断

对照 `CONSENSUS_T-P0-21_MTM列表页摘要卡动作引导与快捷回退统一.md`：

1. 摘要卡出现统一动作引导区：通过
2. 动作会随预设来源/筛选场景变化：通过
3. 空态按钮复用同一套动作定义：通过
4. 摘要卡动作不超过 2 个主动作：通过
5. 前端构建通过：通过

结论：

- `T-P0-21` 达到当前阶段可交付标准
- MTM 主线已从“列表页首屏信息组织统一”推进到“列表页下一步动作引导统一”

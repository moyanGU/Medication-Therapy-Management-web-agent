# ACCEPTANCE_T-P0-14_MTM列表页与详情页筛选联动和路由查询保持

## 一、任务结论

`T-P0-14 MTM 列表页与详情页之间的筛选联动 / 路由查询保持` 已完成本轮最小可交付目标：

- 已让 MTM 列表页把搜索、状态筛选和页码同步到 URL
- 已支持列表页刷新后按查询参数恢复状态
- 已支持从列表页进入详情时保留最小返回上下文
- 已支持详情页返回时恢复原列表查询条件
- 已完成前端构建验证

---

## 二、本轮完成项

### 2.1 文档对齐

已新增：

- `ALIGNMENT_T-P0-14_MTM列表页与详情页筛选联动和路由查询保持.md`
- `CONSENSUS_T-P0-14_MTM列表页与详情页筛选联动和路由查询保持.md`

当前已明确：

- 本轮只做“筛选联动 + 路由查询保持”
- 不新增后端接口
- 不扩展新的筛选字段和高级排序

### 2.2 列表页查询参数保持

已修改：

- `src/pages/MtmServiceCasesPage.vue`

当前列表页已支持：

1. 首次进入时按 `route.query` 恢复 `page`、`search`、`status`
2. 搜索时同步 URL
3. 状态筛选变化时同步 URL
4. 页码变化时同步 URL
5. 刷新页面后保持当前列表上下文
6. 浏览器前进后退时按 URL 恢复列表状态

### 2.3 详情页返回上下文恢复

已修改：

- `src/pages/MtmServiceCaseDetailPage.vue`

当前详情页已支持：

1. 从列表页进入详情时带上 `from=mtm-list`
2. 同时保留 `page`、`search`、`status`
3. 点击返回时优先回到 `/mtm/service-cases`
4. 返回时恢复原来的页码和筛选条件
5. 非列表入口进入详情时保持原有返回逻辑

### 2.4 跳转链路补齐

当前列表页到详情页的跳转已改为：

- 使用带 query 的路由对象进入详情页

当前保留的最小上下文参数为：

1. `page`
2. `search`
3. `status`
4. `from=mtm-list`

---

## 三、验证结果

### 3.1 编辑文件诊断

已检查：

- `src/pages/MtmServiceCasesPage.vue`
- `src/pages/MtmServiceCaseDetailPage.vue`

结果：

- 无新增诊断错误

### 3.2 前端生产构建

已执行：

```bash
npm run build
```

结果：

- 构建成功
- `vue-tsc` 通过
- `vite build` 通过

说明：

- 当前列表页查询参数联动和详情页返回恢复逻辑已通过类型检查和生产构建

---

## 四、已知限制

本轮明确未做：

- 触发来源筛选加入 URL
- 高级排序加入 URL
- 更多列表筛选项
- 首页入口参数联动

原因：

- 本任务只负责最小列表上下文保持
- 这些内容属于后续体验增强

---

## 五、验收判断

对照 `CONSENSUS_T-P0-14_MTM列表页与详情页筛选联动和路由查询保持.md`：

1. 列表页搜索后 URL 会保留查询参数：通过
2. 列表页筛选和页码变化后 URL 会同步更新：通过
3. 刷新列表页后仍能恢复同样的筛选结果：通过
4. 从列表进入详情再返回时，能回到原页码和筛选状态：通过
5. 从首页直接进入详情时，仍保持现有返回逻辑：通过
6. 前端构建通过：通过

结论：

- `T-P0-14` 达到当前阶段可交付标准
- MTM 主线已从“有独立历史列表页”推进到“列表与详情之间的上下文可保持”

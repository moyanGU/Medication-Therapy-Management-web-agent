# CONSENSUS_T-P0-38_MTM列表页主结果区底部结果说明条阅读轨道收口

## 一、任务共识

本轮正式启动的开发任务为：

- `T-P0-38 MTM 列表页主结果区底部结果说明条阅读轨道收口`

目标共识：

- 让主结果区底部结果说明更容易扫读
- 让右侧分页状态更稳定地呈现为辅助信息

---

## 二、范围共识

本任务确定只做以下内容：

1. 收口左侧 `resultSectionCopy` 的阅读宽度
2. 调整右侧 `resultPageStatus` 的容器表现和层级
3. 稳定底部说明条“左主右辅”的视觉关系

明确不做：

- 修改 `resultSectionCopy` 和 `resultPageStatus` 的文案逻辑
- 修改分页按钮、分页行为和底部分页条
- 修改顶部摘要卡、空态区、接口、路由和 `src/utils/mtm.ts`
- 修改列表项卡片、首页和详情页

---

## 三、层级共识

本轮底部说明条层级继续保持：

1. 左侧主结果说明
2. 右侧辅助分页状态

补充共识：

- 不改变结构位置
- 不把分页状态抬升成主内容
- 只让主次关系更清晰

---

## 四、实现方式共识

本轮实现方式确定为：

1. 仅在底部说明条模板结构和样式类上做轻量调整
2. 通过阅读宽度和辅助块表现收口视觉层级
3. 不新增新的业务状态和交互逻辑

原因共识：

- 当前底部说明条语义已经明确
- 本轮属于稳定期的展示层细收口
- 最小模板调整最安全、最可回滚

---

## 五、实现文件共识

本任务确定修改：

1. `src/pages/MtmServiceCasesPage.vue`

本轮不修改：

1. `src/utils/mtm.ts`
2. `src/types/mtm.ts`
3. `src/pages/DashboardPage.vue`
4. `src/pages/MtmServiceCaseDetailPage.vue`
5. `src/api/mtm.ts`
6. 后端 `apps.mtm` 代码

---

## 六、验收标准

本任务完成后，必须满足以下验收条件：

1. 左侧结果说明阅读轨道更清晰
2. 右侧分页状态更像辅助信息
3. 文案逻辑与分页行为保持不变
4. 只修改 `src/pages/MtmServiceCasesPage.vue`
5. 前端诊断通过
6. `npm run build` 通过

---

## 七、实施结论

`T-P0-38` 的最终实施方案确定为：

1. 继续保持单文件、小步、可回滚
2. 只收口主结果区底部结果说明条的阅读轨道与辅助状态层级
3. 不碰文案来源、分页逻辑、空态区和数据流
4. 用最小模板调整完成本轮收口

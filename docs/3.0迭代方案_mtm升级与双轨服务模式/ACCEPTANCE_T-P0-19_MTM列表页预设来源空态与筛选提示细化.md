# ACCEPTANCE_T-P0-19_MTM列表页预设来源空态与筛选提示细化

## 一、任务结论

`T-P0-19 MTM 列表页预设来源空态与筛选提示细化` 已完成本轮最小可交付目标：

- 已细化 MTM 列表页的筛选摘要提示
- 已细化 MTM 列表页的空态标题与说明
- 已让预设来源场景下的空态更贴近首页入口语义
- 已完成前端构建验证

---

## 二、本轮完成项

### 2.1 文档对齐

已新增：

- `ALIGNMENT_T-P0-19_MTM列表页预设来源空态与筛选提示细化.md`
- `CONSENSUS_T-P0-19_MTM列表页预设来源空态与筛选提示细化.md`

当前已明确：

- 本轮只打磨列表页提示与空态文案
- 不新增后端接口
- 不新增新的筛选与排序能力

### 2.2 共享文案能力

已修改：

- `src/utils/mtm.ts`

当前已补齐：

1. 列表页当前条件摘要生成函数
2. 列表页空态标题生成函数
3. 列表页空态说明生成函数

当前文案生成已覆盖：

- 搜索词
- 状态筛选
- 触发来源筛选
- 排序方式
- 预设来源场景

### 2.3 列表页提示细化

已修改：

- `src/pages/MtmServiceCasesPage.vue`

当前列表页已支持：

1. 在页头显示“当前条件”摘要
2. 根据预设来源显示更贴近首页语义的列表说明
3. 根据不同空态原因显示不同标题
4. 根据不同空态原因显示不同说明

当前空态已区分：

1. 无历史记录
2. 首页当前阶段预设进入但无结果
3. 首页同类触发预设进入但无结果
4. 手动搜索无结果
5. 状态/触发来源筛选无结果

### 2.4 现有链路保持

本轮继续保留：

1. `preset_source` 识别逻辑
2. 手动改筛选时清除预设来源标识
3. 列表进入详情再返回时恢复来源标识

说明：

- 本轮没有破坏 `T-P0-18` 已完成的来源提示与返回恢复能力

---

## 三、验证结果

### 3.1 编辑文件诊断

已检查：

- `src/pages/MtmServiceCasesPage.vue`
- `src/utils/mtm.ts`

结果：

- 无新增诊断错误

### 3.2 前端生产构建

已执行：

```bash
npm run build
```

结果：

- 构建成功
- `vue-tsc` 通过
- `vite build` 通过
- `MtmServiceCasesPage` 与 `MtmServiceCaseDetailPage` 构建产物正常生成

说明：

- 当前筛选摘要与空态细化逻辑已通过类型检查和生产构建

---

## 四、已知限制

本轮明确未做：

- 首页入口文案同步展示更复杂的来源说明
- 列表页空态加入更多差异化插图或样式
- 更复杂的多条件优先级提示系统

原因：

- 本任务只负责最小人话提示细化
- 这些内容属于后续体验优化

---

## 五、验收判断

对照 `CONSENSUS_T-P0-19_MTM列表页预设来源空态与筛选提示细化.md`：

1. 列表页页头能更清楚说明当前筛选和查看方式：通过
2. 预设来源为空时的空态文案更贴近首页入口语义：通过
3. 搜索或手动筛选为空时，能更准确解释原因：通过
4. 无历史记录时仍保留清晰的引导动作：通过
5. 前端构建通过：通过

结论：

- `T-P0-19` 达到当前阶段可交付标准
- MTM 主线已从“列表页来源可识别”推进到“列表页结果与空态可解释”

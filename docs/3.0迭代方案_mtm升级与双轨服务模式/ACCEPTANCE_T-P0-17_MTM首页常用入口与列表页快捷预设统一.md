# ACCEPTANCE_T-P0-17_MTM首页常用入口与列表页快捷预设统一

## 一、任务结论

`T-P0-17 MTM 首页常用入口与列表页快捷预设统一` 已完成本轮最小可交付目标：

- 已抽取首页 MTM 列表共享预设 helper
- 已让首页 MTM 区块快捷跳转复用统一预设
- 已在首页“常用入口”中加入 MTM 主入口
- 已让常用入口中的 MTM 主入口复用同一套预设定义
- 已完成前端构建验证

---

## 二、本轮完成项

### 2.1 文档对齐

已新增：

- `ALIGNMENT_T-P0-17_MTM首页常用入口与列表页快捷预设统一.md`
- `CONSENSUS_T-P0-17_MTM首页常用入口与列表页快捷预设统一.md`

当前已明确：

- 本轮只做首页 MTM 入口规则统一
- 不新增后端接口
- 不修改列表页筛选模型和详情页返回机制

### 2.2 共享预设能力

已修改：

- `src/types/mtm.ts`
- `src/utils/mtm.ts`

当前已补齐：

- MTM 列表预设入口类型
- MTM 列表共享路由构造函数
- MTM 列表预设入口生成函数
- 默认排序常量

当前统一预设最小集合包括：

1. 全部服务
2. 当前阶段服务
3. 同类触发服务

### 2.3 首页 MTM 区块统一

已修改：

- `src/pages/DashboardPage.vue`

当前首页 MTM 区块已改为：

1. 快捷跳转通过统一预设数组渲染
2. “查看全部”继续走统一预设中的全部服务入口
3. 保持原有参数化跳转行为不变

### 2.4 首页常用入口统一

当前首页“常用入口”已新增：

- `专业服务` 主入口卡片

该入口当前规则：

1. 优先跳到当前阶段服务
2. 否则跳到同类触发服务
3. 再否则回退到全部服务

并且：

- 标题、描述和跳转目标都来自统一预设口径

---

## 三、验证结果

### 3.1 编辑文件诊断

已检查：

- `src/pages/DashboardPage.vue`
- `src/utils/mtm.ts`
- `src/types/mtm.ts`

结果：

- 无新增诊断错误

### 3.2 前端生产构建

已执行：

```bash
npm run build
```

结果：

- 构建成功
- `vue-tsc` 通过
- `vite build` 通过

说明：

- 当前首页 MTM 入口统一逻辑已通过类型检查和生产构建

---

## 四、已知限制

本轮明确未做：

- 常用入口中增加多个 MTM 预设卡片
- 列表页内部新增预设标签展示
- 详情页显示入口来源说明
- 首页其他区域继续扩展 MTM 导航

原因：

- 本任务只负责首页 MTM 入口统一
- 这些内容属于后续导航体验增强

---

## 五、验收判断

对照 `CONSENSUS_T-P0-17_MTM首页常用入口与列表页快捷预设统一.md`：

1. 首页 MTM 区块快捷跳转改为复用统一预设：通过
2. 首页“常用入口”出现 MTM 主入口：通过
3. 常用入口中的 MTM 主入口与 MTM 区块使用同一套预设口径：通过
4. 现有首页 MTM 跳转行为不回退：通过
5. 前端构建通过：通过

结论：

- `T-P0-17` 达到当前阶段可交付标准
- MTM 主线已从“首页入口参数联动”推进到“首页入口规则统一”

# ACCEPTANCE_T-P0-15_MTM列表页触发来源筛选与排序保持

## 一、任务结论

`T-P0-15 MTM 列表页触发来源筛选与排序保持` 已完成本轮最小可交付目标：

- 已在 MTM 列表页增加触发来源筛选
- 已在 MTM 列表页增加排序方式选择
- 已将 `trigger_source` 与 `ordering` 纳入 URL 查询保持
- 已将这两个新增上下文纳入详情页返回恢复逻辑
- 已完成前端构建验证

---

## 二、本轮完成项

### 2.1 文档对齐

已新增：

- `ALIGNMENT_T-P0-15_MTM列表页触发来源筛选与排序保持.md`
- `CONSENSUS_T-P0-15_MTM列表页触发来源筛选与排序保持.md`

当前已明确：

- 本轮只做“触发来源筛选 + 排序保持”
- 不新增后端接口
- 不扩展更多高级筛选和复杂排序面板

### 2.2 共享类型与校验能力

已修改：

- `src/types/mtm.ts`
- `src/utils/mtm.ts`

当前已补齐：

- MTM 服务单排序类型
- 触发来源合法值集合
- 排序合法值集合
- 排序文案映射
- 触发来源与排序合法性校验函数

### 2.3 列表页增强

已修改：

- `src/pages/MtmServiceCasesPage.vue`

当前列表页已支持：

1. 触发来源筛选
2. 排序方式选择
3. `trigger_source` 同步到 URL
4. `ordering` 同步到 URL
5. 刷新页面后恢复这两个状态
6. 浏览器前进后退时恢复这两个状态

### 2.4 详情页返回上下文恢复

已修改：

- `src/pages/MtmServiceCaseDetailPage.vue`

当前详情页已支持在返回列表页时恢复：

1. `page`
2. `search`
3. `status`
4. `trigger_source`
5. `ordering`

说明：

- 从列表页进入详情后，触发来源筛选和排序方式不会在返回时丢失
- 非列表入口进入详情时，仍保持原有返回逻辑

---

## 三、验证结果

### 3.1 编辑文件诊断

已检查：

- `src/pages/MtmServiceCasesPage.vue`
- `src/pages/MtmServiceCaseDetailPage.vue`
- `src/utils/mtm.ts`
- `src/types/mtm.ts`

结果：

- 无新增诊断错误

### 3.2 前端生产构建

已执行：

```bash
npm run build
```

结果：

- 构建成功
- `vue-tsc` 通过
- `vite build` 通过

说明：

- 当前触发来源筛选、排序保持与详情页返回恢复逻辑已通过类型检查和生产构建

---

## 四、已知限制

本轮明确未做：

- 更多高级筛选项
- 更多排序字段
- 自定义组合排序
- 首页入口直接带入触发来源或排序参数

原因：

- 本任务只负责最小增量增强
- 这些内容属于后续体验扩展

---

## 五、验收判断

对照 `CONSENSUS_T-P0-15_MTM列表页触发来源筛选与排序保持.md`：

1. 列表页可按触发来源筛选：通过
2. 列表页可切换排序方式：通过
3. `trigger_source` 与 `ordering` 会同步到 URL：通过
4. 刷新列表页后这两个状态仍能恢复：通过
5. 从列表进入详情再返回时，这两个状态不会丢：通过
6. 前端构建通过：通过

结论：

- `T-P0-15` 达到当前阶段可交付标准
- MTM 主线已从“基础筛选保持”推进到“更完整的列表筛选与排序保持”

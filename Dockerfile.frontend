# 前端Dockerfile（统一使用 Node 22，满足 Vite 7/相关依赖的 engine 要求）
FROM node:22-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖（包含 devDependencies，确保构建工具如 vue-tsc 可用）
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发环境启动命令
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]